"""Mnemos orchestrator package.

This package owns the memory graph, agent coordination, and LLM calls. The
TypeScript GitHub App service is the only external caller for the HTTP API
exposed by :mod:`codereview.api`.
"""

__version__ = "0.1.0-alpha.0"
